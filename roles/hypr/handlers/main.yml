---
- name: hypr | reload hyprland
  ansible.builtin.command:
    cmd: hyprctl reload
  ignore_errors: true
  when: ansible_env.XDG_SESSION_TYPE == "wayland"

- name: hypr | reload hyprpanel
  ansible.builtin.command:
    cmd: hyprpanel -q && hyprpanel