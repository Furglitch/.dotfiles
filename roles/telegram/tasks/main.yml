---
- name: telegram | ensure telegram-desktop installed
  kewlfft.aur.aur:
    name: telegram-desktop
    state: present
    use: paru
  become_user: "{{ target_user }}"
  become_flags: "-H"
  ignore_errors: true
