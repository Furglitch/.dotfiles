---
- name: heroic-games-launcher | ensure heroic-games-launcher-bin installed
  kewlfft.aur.aur:
    name: heroic-games-launcher-bin
    state: present
    use: paru
  become_user: "{{ target_user }}"
  become_flags: "-H"
  ignore_errors: true
